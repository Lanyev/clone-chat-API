//? This is the valiudateUserConversation middleware
// Genereate a middleware to validate if the user is on the conversation

const {
  validateUserOnConversation,
} = require("../participants/participants.controllers");
